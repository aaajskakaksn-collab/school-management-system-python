# Example .env (template) - do NOT commit real secrets
# Copy this file to example/.env or create a .env in project root and set values locally.

# SECRET_KEY should be a long random string in production
SECRET_KEY=change-me-secret
# DATABASE_URL if you want to override the default example/db.sqlite3
DATABASE_URL=sqlite:///example/db.sqlite3
